name: Request Claude Review

on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  issues: write
  pull-requests: write

jobs:
  request-claude:
    runs-on: ubuntu-latest
    steps:
      - name: Ensure Claude review comment exists
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo, number } = context.issue;
            const desiredBody = "@claude review";

            // Avoid duplicate comments
            const comments = await github.paginate(
              github.rest.issues.listComments,
              { owner, repo, issue_number: number, per_page: 100 }
            );
            const alreadyPosted = comments.some(
              (c) => c.user?.login === "github-actions[bot]" && c.body?.trim() === desiredBody
            );
            if (alreadyPosted) {
              core.info("Claude review comment already present; skipping.");
              return;
            }

            await github.rest.issues.createComment({
              owner,
              repo,
              issue_number: number,
              body: desiredBody,
            });
            core.info("Posted Claude review comment.");
